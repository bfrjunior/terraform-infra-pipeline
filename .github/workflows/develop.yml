name: "DEV DEPLOY"

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: dev
      aws-assume-role-arn: "arn:aws:iam::409290017848:role/github-action-bfrjunior-pipeline"
      aws-region: "sa-east-1"
      aws-statefile-s3-bucket: "bfrjunior-sa-east-1-terraform-statefile"
      aws-lock-dynamodb-table: "bfrjunior-sa-east-1-terraform-lock"